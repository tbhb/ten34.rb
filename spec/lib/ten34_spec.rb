# frozen_string_literal: true

rspec.describe ten34 do
  it 'has a version number' do
    expect(Ten34.version).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
